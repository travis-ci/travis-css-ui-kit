import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { color } from '@storybook/addon-knobs';

<Meta title="Assets/Icons" />

export const template = icon => {
  const colorValue = color('Color', '#000');
  return `<i class="tci-icon tci-icon--${icon}" style="color: ${colorValue}" />`;
};

# List of available Icons

<Preview>
  <Story name="Assembla">{template('assembla')}</Story>
  <Story name="Bitbucket">{template('bitbucket')}</Story>
  <Story name="GitHub">{template('github')}</Story>
  <Story name="Gitlab">{template('gitlab')}</Story>
</Preview>
